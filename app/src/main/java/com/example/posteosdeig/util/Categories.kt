package com.example.posteosdeig.util

import com.example.posteosdeig.R

enum class Categories(val color: Int) {
    MODA(R.color.moda_color),
    DECO(R.color.deco_color),
    SPORT(R.color.sport_color)
}